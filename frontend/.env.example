# Copie este arquivo para .env.local e ajuste conforme o ambiente.
# Em dev, o proxy do vite.config.js ja cuida de /api -> localhost:8080,
# entao essa variavel so importa de fato em producao (build final).
VITE_API_URL=/api
